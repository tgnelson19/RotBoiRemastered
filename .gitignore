__pycache__/
*.py[cod]
.pytest_cache/
.codex-deps/
build/
dist/
*.spec.bak
data/profile.json

# C# port (csharp/)
csharp/**/bin/
csharp/**/obj/
*.user

# .NET build output -- regenerated by `dotnet build`, never hand-authored.
# Matches at any depth, so it also covers RotBoiRemastered/Content/obj/ (the
# MonoGame content builder's own intermediate output).
bin/
obj/